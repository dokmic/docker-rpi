services:
  rpi:
    build:
      args:
        - image=$IMAGE
        - kernel=$KERNEL
      context: .
      entitlements:
        - security.insecure
    environment:
      - RPI_PASSWORD
      - RPI_USER=${RPI_USER:-pi}
    image: rpi:latest
