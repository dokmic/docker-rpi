services:
  rpi:
    build:
      args:
        - image=$IMAGE
        - kernel=$KERNEL
      context: .
      entitlements:
        - security.insecure
    environment:
      - RPI_SSH
      - RPI_USER
      - RPI_PASSWORD
    image: rpi:latest
    ports:
      - 2222:22
