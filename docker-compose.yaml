services:
  rpi:
    build:
      args:
        - image=$IMAGE
        - kernel=$KERNEL
      context: .
      entitlements:
        - security.insecure
    env_file:
      - path: .env
        required: false
    image: rpi:latest
    ports:
      - 2222:22
